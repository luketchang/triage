/**
 * Re-export chat handlers from the new modular structure
 * This file is kept for backward compatibility
 * @deprecated Use handlers/chat-handlers.ts instead
 */

export { cleanupChatHandlers, setupChatHandlers } from "./handlers/chat-handlers.js";
