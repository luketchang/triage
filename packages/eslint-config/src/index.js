const { baseConfig } = require("./base");
const { nodeConfig } = require("./node");
const { typescriptConfig } = require("./typescript");
const { typescriptCheckedConfig } = require("./typescript-checked");

module.exports = {
  baseConfig,
  nodeConfig,
  typescriptConfig,
  typescriptCheckedConfig,
};
